language: php
php:
  - "5.4"
  - "5.3"
before_install:
  - yum list
before_script:
  - php -v
script:
  - php -i

notifications:
  emails:
    - e.ogura@moregames.co.jp
  on_success: always # default: change
  on_failure: always
